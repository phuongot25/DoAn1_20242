<UserControl x:Class="RestaurantManagement.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400">
    <Grid Background="#F0F0F0">
        <Border CornerRadius="10" Background="White" Padding="30" Width="350" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Đăng Ký Tài Khoản" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Họ tên:"/>
                <TextBox Text="{Binding FullName}" Margin="0,5,0,10"/>

                <TextBlock Text="Số điện thoại:"/>
                <TextBox Text="{Binding Phone}" Margin="0,5,0,10"/>

                <TextBlock Text="Email:"/>
                <TextBox Text="{Binding Email}" Margin="0,5,0,10"/>

                <TextBlock Text="Tên đăng nhập:"/>
                <TextBox Text="{Binding Username}" Margin="0,5,0,10"/>

                <TextBlock Text="Mật khẩu:"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,5,0,10"/>

                <TextBlock Text="Xác nhận mật khẩu:"/>
                <PasswordBox x:Name="ConfirmPasswordBox" PasswordChanged="ConfirmPasswordBox_PasswordChanged" Margin="0,5,0,20"/>

                <Button Content="Đăng ký" Command="{Binding RegisterCommand}" Background="#27AE60" Foreground="White" Padding="8"/>
                <TextBlock Text="{Binding RegisterMessage}" Foreground="Red" Margin="0,10,0,0" HorizontalAlignment="Center"/>

                <Button Content="← Quay lại đăng nhập" Command="{Binding NavigateLoginCommand}" Background="#95A5A6" Foreground="White" Padding="6" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

