<UserControl x:Class="RestaurantManagement.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Background="#F0F0F0">
        <Border CornerRadius="10" Background="White" Padding="30" Width="350" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Đăng Nhập" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Tên đăng nhập:" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Username}" Margin="0,5,0,10"/>

                <TextBlock Text="Mật khẩu:" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,5,0,20"/>

                <ComboBox ItemsSource="{Binding Roles}"
          SelectedItem="{Binding SelectedRole}"
          Margin="0,5,0,10"/>


                <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Background="#27AE60" Foreground="White" Padding="8"/>
                <TextBlock Text="{Binding LoginMessage}" Foreground="Red" Margin="0,10,0,0" HorizontalAlignment="Center"/>

                <TextBlock Text="Chưa có tài khoản?" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                <Button Content="Đăng ký ngay" Command="{Binding NavigateRegisterCommand}" Background="#2980B9" Foreground="White" Padding="6" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
