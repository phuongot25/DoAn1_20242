<UserControl x:Class="RestaurantManagement.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Thanh chức năng -->
            <RowDefinition Height="*"/>    <!-- Danh sách món ăn -->
        </Grid.RowDefinitions>

        <!-- Dãy nút chức năng -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="➕ Thêm món" Margin="5" Padding="10,5"/>
            <Button Content="✏️ Sửa món" Margin="5" Padding="10,5"/>
            <Button Content="🗑️ Xóa món" Margin="5" Padding="10,5"/>
            <Button Content="🔍 Tìm món ăn" Margin="5" Padding="10,5"/>
        </StackPanel>

        <!-- Luôn hiển thị danh sách món ăn -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding MonAnList}"
                  AutoGenerateColumns="True"
                  SelectedItem="{Binding SelectedMon}"
                  IsReadOnly="True"/>
    </Grid>
</UserControl>

