<UserControl x:Class="RestaurantManagement.Views.StaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Dãy nút chức năng nhân viên -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="➕ Thêm" Margin="5" Padding="10,5"/>
            <Button Content="✏️ Sửa" Margin="5" Padding="10,5"/>
            <Button Content="🗑️ Xóa" Margin="5" Padding="10,5"/>
            <Button Content="📋 Phân công công việc" Margin="5" Padding="10,5"/>
            <Button Content="📊 Thống kê" Margin="5" Padding="10,5"/>
        </StackPanel>

        <!-- Vùng hiển thị nội dung -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1">
            
        </Border>
    </Grid>
</UserControl>

