<UserControl x:Class="RestaurantManagement.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Thanh nút chức năng -->
            <RowDefinition Height="*"/>
            <!-- Vùng danh sách đơn -->
        </Grid.RowDefinitions>

        <!-- Dãy nút chức năng -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="🧾 Lập đơn hàng" Width="81" FontSize="10" Margin="4"/>
            <Button Content="➕ Thêm món" Width="69" FontSize="10" Margin="4"/>
            <Button Content="✏️ Sửa món" Width="60" FontSize="10" Margin="4"/>
            <Button Content="🗑️ Xóa món" Width="68" FontSize="10" Margin="4"/>
            <Button Content="🔄 Trạng thái" Width="70" FontSize="10" Margin="4"/>
            <Button Content="💰 Thanh toán" Width="70" FontSize="10" Margin="4"/>
            <Button Content="🔍 Tìm hóa đơn" Width="80" FontSize="10" Margin="4"/>
            <Button Content="📊 Thống kê" Width="70" FontSize="10" Margin="4"/>
        </StackPanel>

        <!-- Danh sách đơn hàng -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding DonHangList}"
                  SelectedItem="{Binding SelectedDonHang}"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</UserControl>
